function setup() {
  let canvas = createCanvas(1000,600);
  canvas.parent('p5-canvas-container-game');
  canvas.position(windowWidth / 2 - width / 2, windowHeight /2  - height / 2);
}

function draw(){
  background(220)
}